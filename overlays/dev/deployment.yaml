apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-web-deployment
spec:
  replicas: 8
  template:
    spec:
      containers:
      - name: demo-web
        image: webbutvecklinghelsingborg/gitops:latest
